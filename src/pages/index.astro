---
import { getEntry } from "astro:content";
import Layout from "../layouts/Layout.astro";

const page = await getEntry("pages", "home");
const { Content } = await page.render();
---

<Layout title="Home page" editDate={page.data.editDate}>
    <Content />
</Layout>
